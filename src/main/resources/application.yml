spring:
  application:
    name: taskflow

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  threads:
    virtual:
      enabled: true